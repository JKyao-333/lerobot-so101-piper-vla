# Data

Place private LeRobot datasets under this directory only for local work. Everything except this file is ignored.

Verify `meta/info.json`, camera keys, action representation, task identity, and video readability before training. Do not commit raw Piper trajectories, video, parquet files, device serials, or identifying scene content.

