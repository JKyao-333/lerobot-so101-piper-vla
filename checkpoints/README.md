# Checkpoints

Download `pretrained_model` or adapter directories here for local use. Model files are ignored and must not be committed.

Before rollout, verify that the directory contains the model/config and the matching policy preprocessor/postprocessor files expected by the installed LeRobot version.

