# Media

Public media is intentionally empty. Keep raw camera captures and rollout videos under `media/raw/`, which is ignored. Publish only explicitly approved, non-identifying, compressed demonstrations with a documented source and license.

